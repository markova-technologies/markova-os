{
  "name": "embedding-worker",
  "version": "1.0.0",
  "description": "Markova embedding worker — generates pgvector embeddings for knowledge chunks",
  "main": "index.js",
  "scripts": {
    "start": "node index.js",
    "dev": "node --watch index.js"
  },
  "dependencies": {
    "axios": "^1.7.2",
    "dotenv": "^16.4.5",
    "pg": "^8.12.0"
  }
}
