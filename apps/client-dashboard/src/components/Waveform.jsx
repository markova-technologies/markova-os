// The waveform lives in packages/ui so the dashboard and the docs site render
// the same motif from the same source (Brief §9.3 — build it once).
export { default } from '../../../../packages/ui/waveform/Waveform'
