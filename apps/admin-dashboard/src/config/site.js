/**
 * Admin public + console route map.
 * `/` is marketing landing; console lives under named paths (not `/`).
 */
export const ROUTES = {
  home: '/',
  dashboard: '/dashboard',
  companies: '/companies',
  revenue: '/revenue',
  calls: '/calls',
  health: '/health',
  usage: '/usage',
  tickets: '/tickets',
  audit: '/audit',
  settings: '/settings',
}
